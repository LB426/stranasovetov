<%= form_for(@comment, :url => publication_comment_path(@publication.id,@comment)) do |f| %>
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% @comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <%= f.hidden_field :publication_id, :value => @publication.id %>
  <div class="field">
    <%= f.text_area :body, :cols => "80", :rows => "5" %>
  </div>
  <%= recaptcha_tags unless current_user %>
  <div class="actions">
    <%= f.submit "Добавить комментарий" %>
  </div>
<% end %>
