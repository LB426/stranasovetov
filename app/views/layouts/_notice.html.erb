<% [:notice, :error, :success, :alert].each do |key| %>
  <% unless flash[key].blank? %>
  	<div id="content">
	    <p class="<%= key %>">
	      <%= flash[key] %>
	    </p>
	  </div>
  <% end%>
<% end %>
